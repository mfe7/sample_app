<% provide(:title, 'Credit Card') %>
<%= form_tag swiped_path do %>
<h2>By submitting your credit card information, you are accepting the one-time charge of $<%= ticket_price %>.00 if the concert occurs.</h2>
  <article align=center>
    <label class="amount">
      <span>Single Ticket Price: $<%= ticket_price %>.00</span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/v2/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="1 ticket"
          data-amount="<%= ticket_price * 100 %>"></script>
<% end %>