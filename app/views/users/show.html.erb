<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
      <h2>
      	purchased <%= @user.tickets_purchased %> tickets.
      </h2>
      <h3>Excitement...<%= @user.excite %></h3>
    </section>
  </aside>
</div>